## Tasks

- [ ] Add a script that derives sanitized shareable content for managed files from `main`.
- [ ] Wire `check:shareable` to verify that `shareable` matches the sanitized projection.
- [ ] Update branch workflow docs to use the sanitization step before pushing `shareable`.
- [ ] Run the shareable safety check and the full repo closeout path after the workflow change.
