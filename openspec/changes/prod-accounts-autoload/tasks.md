## Tasks

- [ ] Add the prod Accounts autoload source and latest-file endpoints.
- [ ] Add the prod Accounts launcher script and wire it to the fresh reviewer runtime.
- [ ] Add regression coverage for the prod Accounts autoload and recent-file mapping.
- [ ] Update reviewer docs and contract checks to describe the canonical prod Accounts path.
- [ ] Run `npm run check`, `npm run check:windows`, and `npm run smoke:ui:local`.
