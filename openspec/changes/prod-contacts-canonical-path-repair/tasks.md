## Tasks

- [ ] Update the prod launcher to repair any legacy download-prefixed prod output into the canonical prod root.
- [ ] Pass the canonical prod Contacts CSV path through reviewer startup so the server reads the canonical prod tree.
- [ ] Add regression coverage for the repair helper and canonical prod path.
- [ ] Update reviewer docs and handoff notes to describe the canonical prod root.
- [ ] Update scheduler docs and feature coverage to describe the canonical prod root and prod launch URL.
- [ ] Run `npm run check`, `npm run check:windows`, and `npm run smoke:ui:local` in the reviewer repo.
- [ ] Run the scheduler repo checks that cover the prod job wiring.
