## Tasks

- [ ] Replace the staging and prod Contact SOQL files with the exact approved query-only field set.
- [ ] Update the reviewer contract regression so it asserts the exact Contact export schema and still proves the dataset loads and scores normally.
- [ ] Update the scheduler contract regression so the seeded Contacts job query reflects the exact approved field set.
- [ ] Run the reviewer repo checks and smoke coverage.
- [ ] Run the scheduler repo checks that cover the prod/staging Contacts job wiring.
