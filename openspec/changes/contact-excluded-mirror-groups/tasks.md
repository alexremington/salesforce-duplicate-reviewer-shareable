## Tasks

- [ ] Add Contact excluded-group spec coverage for hard-zero mirror-veto pairs.
- [ ] Extend matching output to emit explicit excluded groups alongside normal duplicate groups.
- [ ] Render excluded groups with distinct styling, reason text, and a filterable status in Match Groups.
- [ ] Block excluded groups from Contact merge queue entry while preserving review decisions and training labels.
- [ ] Update dataset export precedence for excluded-only rows versus overlapping normal groups.
- [ ] Add contract and Playwright regressions, then run `npm run check`, `npm run check:windows`, and `npm run smoke:ui:local`.
