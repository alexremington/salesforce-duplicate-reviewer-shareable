## Tasks

- [x] Add a shared stale-auth merge warning helper in the merge UI.
- [x] Disable the merge review entry and confirm actions when Salesforce auth is blocked.
- [x] Keep `startMergeReviewSession` from starting premerge checks when auth is stale.
- [x] Extend the Playwright smoke regression to assert the blocked UI, warning copy, and zero premerge requests.
- [x] Run `npm run check` and `npm run smoke:ui:local`.
