# Multi-Group Merge Review Confirmation

- Hume mockup: `hume-multi-group-merge-review.png`
- Built implementation: `implemented-multi-group-merge-review.png`
- Mockup source: `multi-group-review-mockup.html`
- Hume state shown: read-only `Review before confirming` flow for multiple queued contact groups, using the same left-rail navigation pattern as `Evaluate` plus `Previous` and `Next` paging within the review panel.
- Built state shown: the implemented multi-group review pane captured from the running app after the feature build passed local smoke coverage.
- Historical artifact: `hume-inline-merge-preview.png` is retained as the earlier single-group concept that was superseded during Hume review.
