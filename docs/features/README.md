# Feature Briefs

Create tracked feature work with:

```bash
npm run feature:new -- feature-slug --title "Feature Title"
```

Each generated brief is linked from `feature-test-manifest.json`. Release checks require shipped features to include requirements, acceptance criteria, Hume design review notes when UI is visible, cross-platform status, and test evidence.
