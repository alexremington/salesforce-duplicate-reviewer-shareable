# Duplicate Reviewer Backlog

## Next

- Validate Contact merge end to end in a Windows 11 Arm VM.
- Expand Playwright coverage for changing the master Contact and selecting field-level retained values.
- Add a small export or review summary for selected merge retained values before executing a merge.
- Add a calibration summary for Evaluate mode so model feedback trends are easier to review.

## Later

- Add more keyboard shortcut discoverability where it does not crowd the workspace.
- Add richer CSV header validation and repair guidance.
- Revisit Account merge only if downstream business rules are explicitly scoped and approved.

## Recurring

- Keep Account merge disabled while Accounts remain evaluation-only.
- Re-run UI smoke tests after visible workspace changes.
- Re-run Windows portability checks after launcher or startup changes.
- Keep public-safe docs and branches free of local data, private configuration, and leftover mirror-repair scratch state.
